Freebuff Ads/font patcher! Removes all the Ads from the desktop client and patches the font to JetBrains Mono if its installed (optional)

I update this every patch in case the loader itself needs an update just check for a new loader but I update the patch list per gist 
so it should always work in case it breaks write me a pm :3 


(i made it simple exe for simple use its no virus you can check on virustotal or something else if you want)
best regards 
Phil
